apiVersion: ceph.rook.io/v1
kind: CephCluster
metadata:
  name: rook-ceph
  namespace: rook-ceph
spec:
  cephVersion:
    allowUnsupported: false
    image: quay.io/ceph/ceph:v20.2.2
  dashboard:
    enabled: true
  dataDirHostPath: /var/lib/rook
  mgr:
    count: 2
  mon:
    allowMultiplePerNode: false
    count: 3
  placement:
    all:
      nodeAffinity:
        requiredDuringSchedulingIgnoredDuringExecution:
          nodeSelectorTerms:
            - matchExpressions:
                - key: kubernetes.io/hostname
                  operator: In
                  values:
                    - ceph-worker1
                    - ceph-worker2
                    - ceph-worker3
  resources:
    mgr:
      limits:
        cpu: "1"
        memory: 2Gi
      requests:
        cpu: 500m
        memory: 1Gi
    mon:
      limits:
        cpu: "1"
        memory: 2Gi
      requests:
        cpu: 500m
        memory: 1Gi
    osd:
      limits:
        cpu: "2"
        memory: 4Gi
      requests:
        cpu: "1"
        memory: 2Gi
  storage:
    nodes:
      - config:
          metadataDevice: /dev/disk/by-id/scsi-0QEMU_QEMU_HARDDISK_incus_ceph_meta_disk
        devices:
          - name: /dev/disk/by-id/scsi-0QEMU_QEMU_HARDDISK_incus_ceph_disk_01
          - name: /dev/disk/by-id/scsi-0QEMU_QEMU_HARDDISK_incus_ceph_disk_02
        name: ceph-worker1
      - config:
          metadataDevice: /dev/disk/by-id/scsi-0QEMU_QEMU_HARDDISK_incus_ceph_meta_disk
        devices:
          - name: /dev/disk/by-id/scsi-0QEMU_QEMU_HARDDISK_incus_ceph_disk_01
          - name: /dev/disk/by-id/scsi-0QEMU_QEMU_HARDDISK_incus_ceph_disk_02
        name: ceph-worker2
      - config:
          metadataDevice: /dev/disk/by-id/scsi-0QEMU_QEMU_HARDDISK_incus_ceph_meta_disk
        devices:
          - name: /dev/disk/by-id/scsi-0QEMU_QEMU_HARDDISK_incus_ceph_disk_01
          - name: /dev/disk/by-id/scsi-0QEMU_QEMU_HARDDISK_incus_ceph_disk_02
        name: ceph-worker3
    useAllNodes: false
